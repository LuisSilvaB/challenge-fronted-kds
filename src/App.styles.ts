import styled from "styled-components"; 

export const AppStyle = styled.main`
    position: relative;
    box-sizing: border-box;
    display: flex; 
    width: 100vw;
    height: 100vh;
    margin: 0;
`